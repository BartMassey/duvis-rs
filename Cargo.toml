[package]
name = "duvis"
version = "0.1.0"
edition = "2024"
description = "ASCII visualization of du(1) disk usage output"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }

[profile.release]
opt-level = 3
lto = "thin"
